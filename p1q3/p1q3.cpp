#include<iostream>
#include<cmath>
#include<stdio.h>
#include<algorithm>
#include<queue>

using namespace std;

int main(int argc, char * argv[]){
    queue<char> pilha;
    int t1,t2,p,n;
    char pratos[100004];
    cin >> p >> t1 >> t2 >> n;
    for(int i = 0; i < n; i++){
        string cor;
        cin >> cor;
        pratos[i] = cor[0];
    }
    while(true){
        pilha.push(pratos[i]);c
    }	
}
